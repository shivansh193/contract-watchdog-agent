.venv/
__pycache__/
*.pyc
.env
outbox/*.jsonl
outbox/*.json
# ...except the two files the deployed frontend reads as its seed data —
# a real, verified snapshot committed so the live Vercel URL shows actual
# agent output instead of an empty dashboard.
!outbox/portfolio_snapshot.json
!outbox/decision_log.jsonl
.pytest_cache/
*.egg-info/

# frontend (Next.js)
frontend/node_modules/
frontend/.next/
frontend/next-env.d.ts
frontend/.env*.local
