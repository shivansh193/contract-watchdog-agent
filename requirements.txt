strands-agents>=1.55.0
python-dotenv>=1.0.0

# Uncomment the line for whichever model provider you use (see .env.example):
# google-genai>=0.3.0        # MODEL_PROVIDER=gemini (default)
# boto3>=1.34.0              # MODEL_PROVIDER=bedrock
# ollama>=0.3.0               # MODEL_PROVIDER=ollama
